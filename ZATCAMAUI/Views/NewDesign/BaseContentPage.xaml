<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="ZATCAMAUI.Views.NewDesign.BaseContentPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ResourceManager="clr-namespace:ZATCAMAUI"
    xmlns:controls="clr-namespace:ZATCAMAUI.Core.CustomControls"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    ios:Page.UseSafeArea="true"
    BackgroundImageSource="partials_background.png"
    FlowDirection="{Binding AppDirection}"
    NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="auto,*">

        <Border
            x:Name="MainContainer"
            Grid.Row="1"
            Padding="0"
            x:FieldModifier="public"
            BackgroundColor="White"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 15,15,0,0" />

        <controls:Header x:Name="titel" TitleText="{x:Static ResourceManager:AppResources.Home}" />

        <controls:Loader Grid.RowSpan="2" IsVisible="{Binding IsLoading}" />

        <controls:MsgView
            Grid.RowSpan="2"
            BackgroundColor="White"
            IsVisible="{Binding IsShowMsgView}"
            VerticalOptions="EndAndExpand" />

        <!--  Bottom Sheet List  -->
        <controls:ListBottomSheet Grid.Row="1" IsVisible="{Binding IsShowBottomSheet}" />

    </Grid>
</ContentPage>
