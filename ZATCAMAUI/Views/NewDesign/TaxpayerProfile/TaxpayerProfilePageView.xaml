<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ZATCAMAUI.Views.NewDesign.TaxpayerProfile.TaxpayerProfilePageView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ResourceManager="clr-namespace:ZATCAMAUI"
    xmlns:custom="clr-namespace:ZATCAMAUI.Core.CustomControls"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    ios:Page.UseSafeArea="true"
    BackgroundImageSource="partials_background.png"
    FlowDirection="{Binding AppDirection}"
    NavigationPage.HasNavigationBar="False"
    mc:Ignorable="d">

    <ContentPage.Content>
        <Grid RowDefinitions="auto,*" RowSpacing="0">

            <!--#region Header-->
            <Grid Padding="20,15" ColumnDefinitions="*,3*,*">

                <StackLayout>

                    <!--  Back Button  -->
                    <Image
                        HorizontalOptions="Start"
                        Rotation="0"
                        Source="whiteRightBack.png"
                        VerticalOptions="CenterAndExpand">

                        <Image.Triggers>
                            <DataTrigger
                                Binding="{Binding AppDirection}"
                                TargetType="Image"
                                Value="LeftToRight">
                                <Setter Property="Rotation" Value="180" />
                            </DataTrigger>
                        </Image.Triggers>

                    </Image>

                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding BackCommand}" />
                    </StackLayout.GestureRecognizers>

                </StackLayout>

                <!--  Header Label  -->
                <Label
                    Grid.Column="1"
                    FontSize="20"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center"
                    Style="{StaticResource SectionTitleWhiteBold}"
                    Text="{x:Static ResourceManager:AppResources.NDProfile}"
                    VerticalOptions="Center" />

            </Grid>
            <!--#endregion Header-->

            <Border
                Grid.Row="1"
                Padding="20,5,20,0"
                Style="{StaticResource CommonTopCurves}">


                <StackLayout>

                    <Label
                        HorizontalOptions="StartAndExpand"
                        Style="{StaticResource LoginMenuTitleWhite}"
                        Text="{Binding TPProfileNameLbl}"
                        TextColor="{StaticResource BlackLabelTextColor}" />

                    <Label
                        Margin="0,0,0,0"
                        HorizontalOptions="StartAndExpand"
                        Style="{StaticResource BodyLabelGrey}"
                        Text="{Binding ResidenceText}" />

                    <ScrollView
                        HorizontalOptions="FillAndExpand"
                        VerticalOptions="FillAndExpand"
                        VerticalScrollBarVisibility="Never">

                        <StackLayout>

                            <Border
                                Padding="10"
                                Stroke="{StaticResource NeutralLightGrey}"
                                StrokeShape="RoundRectangle 16,16,16,16">

                                <StackLayout Orientation="Horizontal">

                                    <StackLayout>

                                        <Label
                                            HorizontalOptions="Start"
                                            Style="{StaticResource ValueLabelGrey}"
                                            Text="{x:Static ResourceManager:AppResources.ZTERReportDetailTIN}" />

                                        <Label
                                            Margin="0,0,0,0"
                                            Style="{StaticResource ValueLabelBlackMedium}"
                                            Text="{Binding TINLabel}" />

                                    </StackLayout>

                                    <Border
                                        Padding="10,0"
                                        BackgroundColor="{StaticResource OpacityPrimary}"
                                        HeightRequest="{OnPlatform Android=50,
                                                                   iOS=32}"
                                        HorizontalOptions="EndAndExpand"
                                        StrokeShape="RoundRectangle 14,14,14,14"
                                        StrokeThickness="1"
                                        VerticalOptions="CenterAndExpand"
                                        WidthRequest="100">

                                        <Label
                                            HorizontalOptions="Center"
                                            HorizontalTextAlignment="Center"
                                            Style="{StaticResource CaptionLabelGrey}"
                                            Text="{Binding TinStatusLabelText}"
                                            TextColor="{StaticResource Primary}"
                                            VerticalTextAlignment="Center" />

                                    </Border>

                                </StackLayout>

                            </Border>

                            <Label
                                Margin="0,16"
                                HorizontalOptions="StartAndExpand"
                                Style="{StaticResource TPProfileLabelBlackBold}"
                                Text="{x:Static ResourceManager:AppResources.ZZZContactInformation}" />

                            <StackLayout Spacing="10">

                                <StackLayout Orientation="Horizontal">

                                    <Label Style="{StaticResource CaptionLabelGrey}" Text="{x:Static ResourceManager:AppResources.MobileNumber}" />

                                    <Image HorizontalOptions="EndAndExpand" Source="ic_Edit.png">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnMobileEditCommand}" />
                                        </Image.GestureRecognizers>
                                    </Image>
                                </StackLayout>

                                <custom:CustomSfTextInputView OutlineCornerRadius="8">
                                    <custom:CustomSfTextInputView.TextInputView>
                                        <custom:GAZTBorderlessEntry
                                            x:Name="MobileNumberCodeEntry"
                                            Placeholder="{x:Static ResourceManager:AppResources.NDConfirmNewEmail}"
                                            HorizontalOptions="FillAndExpand"
                                            InputTransparent="True"
                                            IsReadOnly="True"
                                            Style="{StaticResource DefaultEntry}"
                                            Text="{Binding MobileNumber}"
                                            TextColor="{StaticResource PrimaryColor}"
                                            VerticalTextAlignment="Center" />

                                    </custom:CustomSfTextInputView.TextInputView>
                                </custom:CustomSfTextInputView>

                            </StackLayout>

                            <StackLayout Spacing="10">

                                <StackLayout Orientation="Horizontal">
                                    <Label Style="{StaticResource CaptionLabelGrey}"
                                           Text="{x:Static ResourceManager:AppResources.NDEmail}" />

                                    <Image HorizontalOptions="EndAndExpand" Source="ic_Edit.png">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnEmailEditCommand}" />
                                        </Image.GestureRecognizers>
                                    </Image>
                                </StackLayout>

                                <custom:CustomSfTextInputView OutlineCornerRadius="8">
                                    <custom:CustomSfTextInputView.TextInputView>
                                         <custom:GAZTBorderlessEntry
                                            InputTransparent="True"
                                            IsReadOnly="True"
                                            Style="{StaticResource DefaultEntry}"
                                            Text="{Binding EmailEntry}" />

                                    </custom:CustomSfTextInputView.TextInputView>
                                </custom:CustomSfTextInputView>

                            </StackLayout>

                            <StackLayout Spacing="10">

                                <StackLayout Orientation="Horizontal">

                                    <Label Style="{StaticResource CaptionLabelGrey}"
                                           Text="{x:Static ResourceManager:AppResources.Password}" />

                                    <Image HorizontalOptions="EndAndExpand" Source="ic_Edit.png">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnPasswordEditCommand}" />
                                        </Image.GestureRecognizers>

                                    </Image>

                                </StackLayout>

                                <custom:CustomSfTextInputView OutlineCornerRadius="8">
                                    <custom:CustomSfTextInputView.TextInputView>
                                         <custom:GAZTBorderlessEntry
                                            x:Name="Password"
                                            Placeholder="{x:Static ResourceManager:AppResources.Password}"
                                            InputTransparent="True"
                                            IsPassword="True"
                                            IsReadOnly="True"
                                            Style="{StaticResource DefaultEntry}"
                                            Text="{Binding PasswordEntry}" />

                                    </custom:CustomSfTextInputView.TextInputView>
                                </custom:CustomSfTextInputView>

                            </StackLayout>

                            <Border
                                Padding="16"
                                Stroke="{StaticResource NeutralLightGrey}"
                                StrokeShape="RoundRectangle 16,16,16,16">
                                <StackLayout Orientation="Horizontal">
                                    <StackLayout>

                                        <Label
                                            Margin="0,0,0,0"
                                            Style="{StaticResource ValueLabelBlackMedium}"
                                            Text="{x:Static ResourceManager:AppResources.ManagerDetails}" />
                                    </StackLayout>

                                    <Image HorizontalOptions="EndAndExpand" Source="ic_Edit.png">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnManagerDetailsCommand}" />
                                        </Image.GestureRecognizers>
                                    </Image>
                                </StackLayout>
                            </Border>

                        </StackLayout>

                    </ScrollView>

                </StackLayout>

            </Border>

            <!--  Loader  -->
            <custom:Loader Grid.RowSpan="2" IsVisible="{Binding IsLoading}" />

        </Grid>
    </ContentPage.Content>
</ContentPage>