<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:ResourceManager="clr-namespace:EGAZT"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms" xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView" xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    x:Class="EGAZT.Controls.PickerWithSearch">
        <ContentView.Resources>
        <ResourceDictionary>
          
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Views\SyncFusionEnabledViews\CommonStyle\StylesNewUI.xaml" />
                <ResourceDictionary Source="..\Views\SyncFusionEnabledViews\CommonStyle\Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
                   <Style TargetType="Label">
                <Setter Property="FontFamily" Value="{StaticResource SomarBold}"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource SomarBold}"/>
            </Style>
        </ResourceDictionary>
    </ContentView.Resources>
     <Grid Grid.Row="0" VerticalOptions="FillAndExpand"   Margin="0,0,0,0">
                            <Grid.GestureRecognizers >
                                <TapGestureRecognizer Command="{Binding ClosePickerCommand}"></TapGestureRecognizer>
                            </Grid.GestureRecognizers>
                            <StackLayout Grid.Row="0" Grid.Column="0" Orientation="Vertical" BackgroundColor="Black" Opacity="0.4" VerticalOptions="FillAndExpand">
                </StackLayout>
                <yummy:PancakeView  Style="{StaticResource CommonTopCurves}" CornerRadius="15,15,0,0" VerticalOptions="EndAndExpand" Padding="20,20,20,10" HasShadow="False" BackgroundColor="White">

        <Grid RowDefinitions="auto,*,auto" VerticalOptions="FillAndExpand" Margin="0,0,0,0">
            <SearchBar Margin="0,0,3,0" x:Name="PortsSearchBar" Text="{Binding SearchTxt}" TextColor="{StaticResource Primary}" Grid.Row="0" Placeholder="{x:Static ResourceManager:AppResources.ZSearch}" FontFamily="{StaticResource SomarBold}">
                <SearchBar.Behaviors>
                                                  <xct:EventToCommandBehavior EventName="TextChanged" Command="{Binding SearchInPortsCommand}" CommandParameter="{Binding Text, Source={x:Reference PortsSearchBar}}">
                                                                                             

                                                        </xct:EventToCommandBehavior>
                </SearchBar.Behaviors>
            </SearchBar>
                <Grid Grid.Row="1">
                    <CollectionView x:Name="PortSearchlst" VerticalScrollBarVisibility="Never" IsVisible="{Binding IsPickerOpened}"  ItemsSource="{Binding Ports}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                 <Grid Margin="7" Padding="10,2,10,2">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding BindingContext.PortSelectionChangedCommand, Source={x:Reference PortSearchlst}}" CommandParameter="{Binding .}"></TapGestureRecognizer>
                            </Grid.GestureRecognizers>
                <Label Text="{Binding Name}"  HorizontalTextAlignment="Start" Style="{StaticResource BodyLabelBlack}"></Label>
                  </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
</Grid>
            <buttons:SfButton Grid.Row="2" FontSize="16" BackgroundColor="White" TextColor="#0996D4" BorderColor="{StaticResource PrimaryColor}" Command="{Binding ClosePickerCommand}"  VerticalOptions="CenterAndExpand"   Text="{x:Static ResourceManager:AppResources.ZClose}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,10" />

        </Grid>
    </yummy:PancakeView>
</Grid>

</ContentView>
