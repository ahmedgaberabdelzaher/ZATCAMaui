<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FlowDirection="LeftToRight"
            
              xmlns:local="clr-namespace:GAZT;assembly:GAZT"
               Title="{x:Static local:AppResources.ZVatTermsAndConditions}"
             x:Class="GAZT.Views.NewViews.SignUpTAndCViewPage">
    <ContentPage.Content>
        <StackLayout>
            
                <WebView x:Name="TCWebView" HeightRequest="820" WidthRequest="1000" />
                <StackLayout HeightRequest="80" WidthRequest="1000" Orientation="Horizontal">
                    <CheckBox x:Name="chkTAndC"   IsChecked="{Binding IschkTAndC}" VerticalOptions="Start"  Margin="10,0,0,0" >

                    </CheckBox>
                <Label Text="{x:Static local:AppResources.ZZIagreeontermsAndconditions}"  HorizontalOptions="Start" VerticalOptions="StartAndExpand"  Style="{StaticResource GAZTGrayLabelStyleForCaptionFont}" Margin="0,10,10,0"/>

                </StackLayout>
            <StackLayout HeightRequest="100" WidthRequest="1000">
                <Button Text="{x:Static local:AppResources.Submit}" Command="{Binding OnSubmitClicked}" Style="{StaticResource GAZTVerifyButton}" IsEnabled="{Binding IsButtonEnabled}" VerticalOptions="CenterAndExpand" ></Button>

            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>