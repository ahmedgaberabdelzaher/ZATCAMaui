<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
                       xmlns:ResourceManager="clr-namespace:EGAZT"
    xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    xmlns:custom="clr-namespace:GAZT"
    xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
    x:Class="EGAZT.Views.NewDesign.VATDeclarationPages.ShowVatInformationForPrivatehealthCare">
    <pages:PopupPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\..\SyncFusionEnabledViews\CommonStyle\StylesNewUI.xaml" />
                <ResourceDictionary Source="..\..\SyncFusionEnabledViews\CommonStyle\Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </pages:PopupPage.Resources>
    <yummy:PancakeView x:Name="MainPanCakeView" Margin="0,130,0,0" CornerRadius="8,8,0,0" HasShadow="True" VerticalOptions="EndAndExpand" BackgroundColor="White" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <custom:CustomLabel Grid.Row="0" Text="{x:Static ResourceManager:AppResources.ZVatPrivateHealthcare}" Style="{StaticResource SectionTitleBlack}"/>

            <StackLayout Spacing="15" Margin="0,0,0,7" Grid.Row="1">
                <Frame CornerRadius="12" HasShadow="False" BorderColor="{StaticResource DarkGrayTextColor}" Padding="15">
                    <StackLayout>
                        <StackLayout Orientation="Horizontal">
                            <Image Source="ic_vat_info">

                            </Image>
                            <custom:CustomLabel x:Name="HeaderTextlbl1ForPrivateCare"   Text="{x:Static ResourceManager:AppResources.ZZZZAmountWithSAR}" Style="{StaticResource BodyLabelBlack}" VerticalOptions="CenterAndExpand">
                                <!--<custom:CustomLabel.Triggers>
                                    <DataTrigger TargetType="custom:CustomLabel" Binding="{Binding Source={x:Reference HeaderTextlbl}, Path=Text.Length}" Value="0">
                                        <Setter Property="IsVisible" Value="False" />
                                    </DataTrigger>
                                </custom:CustomLabel.Triggers>-->
                            </custom:CustomLabel>
                        </StackLayout>
                        <custom:CustomLabel Text="{x:Static ResourceManager:AppResources.ZToolTipPrivateHealthcareAmount}" Style="{StaticResource CaptionLabelGrey}" />
                    </StackLayout>
                </Frame>

                <!--<Frame CornerRadius="12" BorderColor="{StaticResource DarkGrayTextColor}" Padding="15">
                                        <StackLayout>
                                            <StackLayout Orientation="Horizontal">
                                                <Image Source="ic_vat_info">

                                                </Image>
                                                <custom:CustomLabel Text="Adjustments" Style="{StaticResource BodyLabelBlack}" VerticalOptions="CenterAndExpand"/>
                                            </StackLayout>
                                            <custom:CustomLabel Text="Total Amount of Supplies adjustments (excluding VAT paid) made to the previous reporting periods supplies that were sold in KSA, in addition to, nominal supplies and supplies subject to margin scheme" Style="{StaticResource CaptionLabelGrey}" />
                                            <custom:CustomLabel Text="For example returns of goods and services by customers on which you have paid VAT to GAZT, write off of bad debt on standard rated receivables." Style="{StaticResource CaptionLabelGrey}" />
                                        </StackLayout>
                                    </Frame>-->
            </StackLayout>

            <StackLayout Spacing="15" Margin="0,0,0,300" Grid.Row="2">
                <Frame CornerRadius="12" HasShadow="False" BorderColor="{StaticResource DarkGrayTextColor}" Padding="15">
                    <StackLayout>
                        <StackLayout Orientation="Horizontal">
                            <Image Source="ic_vat_info">

                            </Image>
                            <custom:CustomLabel x:Name="HeaderTextlbl2ForPrivateCare"   Text="{x:Static ResourceManager:AppResources.ZZZZAdjustmentWithSAR}" Style="{StaticResource BodyLabelBlack}" VerticalOptions="CenterAndExpand">
                                <!--<custom:CustomLabel.Triggers>
                                    <DataTrigger TargetType="custom:CustomLabel" Binding="{Binding Source={x:Reference HeaderTextlbl}, Path=Text.Length}" Value="0">
                                        <Setter Property="IsVisible" Value="False" />
                                    </DataTrigger>
                                </custom:CustomLabel.Triggers>-->
                            </custom:CustomLabel>
                        </StackLayout>
                        <custom:CustomLabel Text="{x:Static ResourceManager:AppResources.ZToolTipPrivateHealthcareAdjustment}" Style="{StaticResource CaptionLabelGrey}" />
                    </StackLayout>
                </Frame>

                <!--<Frame CornerRadius="12" BorderColor="{StaticResource DarkGrayTextColor}" Padding="15">
                                        <StackLayout>
                                            <StackLayout Orientation="Horizontal">
                                                <Image Source="ic_vat_info">

                                                </Image>
                                                <custom:CustomLabel Text="Adjustments" Style="{StaticResource BodyLabelBlack}" VerticalOptions="CenterAndExpand"/>
                                            </StackLayout>
                                            <custom:CustomLabel Text="Total Amount of Supplies adjustments (excluding VAT paid) made to the previous reporting periods supplies that were sold in KSA, in addition to, nominal supplies and supplies subject to margin scheme" Style="{StaticResource CaptionLabelGrey}" />
                                            <custom:CustomLabel Text="For example returns of goods and services by customers on which you have paid VAT to GAZT, write off of bad debt on standard rated receivables." Style="{StaticResource CaptionLabelGrey}" />
                                        </StackLayout>
                                    </Frame>-->
            </StackLayout>

            <!--<ListView  x:Name="InformationList"
                           Grid.Row="1"
           ItemsSource="{Binding HeaderWithInfoList}"
          HorizontalOptions="FillAndExpand"
            VerticalOptions="FillAndExpand"
            HasUnevenRows="True"
                           
                               BackgroundColor="White"
                     
            SeparatorVisibility="None"
            >

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Spacing="15" Margin="0,0,0,7">
                                <Frame CornerRadius="12" HasShadow="False" BorderColor="{StaticResource DarkGrayTextColor}" Padding="15">
                                    <StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                            <Image Source="ic_vat_info">

                                            </Image>
                                            <custom:CustomLabel x:Name="HeaderTextlbl"   Text="{Binding HeaderText}" Style="{StaticResource BodyLabelBlack}" VerticalOptions="CenterAndExpand">
                                                <custom:CustomLabel.Triggers>
                                                    <DataTrigger TargetType="custom:CustomLabel" Binding="{Binding Source={x:Reference HeaderTextlbl}, Path=Text.Length}" Value="0">
                                                        <Setter Property="IsVisible" Value="False" />
                                                    </DataTrigger>
                                                </custom:CustomLabel.Triggers>
                                            </custom:CustomLabel>
                                        </StackLayout>
                                        <custom:CustomLabel Text="{Binding Message}" Style="{StaticResource CaptionLabelGrey}" />
                                    </StackLayout>
                                </Frame>

                                --><!--<Frame CornerRadius="12" BorderColor="{StaticResource DarkGrayTextColor}" Padding="15">
                                        <StackLayout>
                                            <StackLayout Orientation="Horizontal">
                                                <Image Source="ic_vat_info">

                                                </Image>
                                                <custom:CustomLabel Text="Adjustments" Style="{StaticResource BodyLabelBlack}" VerticalOptions="CenterAndExpand"/>
                                            </StackLayout>
                                            <custom:CustomLabel Text="Total Amount of Supplies adjustments (excluding VAT paid) made to the previous reporting periods supplies that were sold in KSA, in addition to, nominal supplies and supplies subject to margin scheme" Style="{StaticResource CaptionLabelGrey}" />
                                            <custom:CustomLabel Text="For example returns of goods and services by customers on which you have paid VAT to GAZT, write off of bad debt on standard rated receivables." Style="{StaticResource CaptionLabelGrey}" />
                                        </StackLayout>
                                    </Frame>--><!--
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->

            <StackLayout BackgroundColor="White" Grid.Row="3" Margin="0,0,0,0"  VerticalOptions="FillAndExpand">

                <buttons:SfButton x:Name="btnprimary" Clicked="DoneButtonClicked"  VerticalOptions="CenterAndExpand" Margin="10,0,10,0"  Text="{x:Static ResourceManager:AppResources.ZZZZDone}" Style="{StaticResource PrimaryButtonStyle}"/>

                <!--<buttons:SfButton x:Name="FirstLink" Text="{Binding FirstLinkText}" TextColor="{StaticResource NewDullOrangeColor}" Style="{StaticResource PrimaryButtonStyle}" Clicked="FirstLinkClicked" BackgroundColor="Transparent" Margin="0,-10,0,3">
                    <buttons:SfButton.Triggers>
                        <DataTrigger TargetType="buttons:SfButton" Binding="{Binding Source={x:Reference FirstLink}, Path=Text.Length}" Value="0">
                            <Setter Property="IsVisible" Value="False" />
                        </DataTrigger>
                    </buttons:SfButton.Triggers>
                </buttons:SfButton>


                <buttons:SfButton x:Name="SecondLink" Text="{Binding SecondLinkText}" TextColor="{StaticResource NewDullOrangeColor}" Style="{StaticResource PrimaryButtonStyle}" Clicked="SecondLinkClicked" BackgroundColor="Transparent" Margin="0,-10,0,3">
                    <buttons:SfButton.Triggers>
                        <DataTrigger TargetType="buttons:SfButton" Binding="{Binding Source={x:Reference SecondLink}, Path=Text.Length}" Value="0">
                            <Setter Property="IsVisible" Value="False" />
                        </DataTrigger>
                    </buttons:SfButton.Triggers>
                </buttons:SfButton>-->
            </StackLayout>
        </Grid>
    </yummy:PancakeView>
    </pages:PopupPage>