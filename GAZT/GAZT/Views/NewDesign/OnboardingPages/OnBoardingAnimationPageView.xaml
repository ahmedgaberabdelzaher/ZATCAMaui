<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="EGAZT.Views.NewDesign.OnboardingPages.GAZTNewDesignOnBoardingAnimationPageView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behavior="clr-namespace:GAZT.SyncFusionEnabledBehaviors.Behaviors"
    xmlns:rotator="clr-namespace:Syncfusion.SfRotator.XForms;assembly=Syncfusion.SfRotator.XForms"
    xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
    xmlns:core="clr-namespace:Syncfusion.XForms.Core;assembly=Syncfusion.Core.XForms"
    xmlns:gradient="clr-namespace:Syncfusion.XForms.Graphics;assembly=Syncfusion.Core.XForms"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:ResourceManager="clr-namespace:EGAZT"
    ios:Page.UseSafeArea="False"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\..\SyncFusionEnabledViews\CommonStyle\StylesNewUI.xaml" />
            </ResourceDictionary.MergedDictionaries>
           
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <!--  Skip Button  -->
                <RowDefinition Height="96" />
                <RowDefinition Height="130" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                
            </Grid.RowDefinitions>

            <!--  Gradient view  -->
            <gradient:SfGradientView Grid.RowSpan="4" Style="{StaticResource SfGradientStyleForOnbBoarding}" />


            <StackLayout Orientation="Horizontal">
                <!--  Skip button  -->

                <buttons:SfButton
                
                Grid.Row="0"
                Margin="10"
                BackgroundColor="Transparent"
                Command="{Binding SkipCommand}"
                CornerRadius="20"
                FontFamily="{StaticResource SSTArBold}"
                HorizontalOptions="Start"
                IsVisible="{Binding IsSkipButtonVisible}"
                Text="{x:Static ResourceManager:AppResources.NDSkip}"
                TextColor="{x:StaticResource NeutralGreay}"
                VerticalOptions="StartAndExpand"
                 />

                <buttons:SfButton
              
                Grid.Row="0"
                Margin="10"
                BackgroundColor="Transparent"
                CornerRadius="20"
                FontFamily="{StaticResource SSTArBold}"
                HorizontalOptions="EndAndExpand"
                IsVisible="{Binding IsSkipButtonVisible}"
                Text="{Binding LanguageText}"
                TextColor="{x:StaticResource NeutralGreay}"
                VerticalOptions="EndAndExpand"
                Clicked="ChangeLangButton_Clicked" />
            </StackLayout>

            <Image Source="centered.png" Aspect="AspectFit" Grid.Row="1"/>

            <!--  Rotator used to navigate the view  -->
            <rotator:SfRotator
               
                Grid.Row="2"
                Margin="{x:OnIdiom Phone='15,0,15,0',
                                   Desktop='0',
                                   Tablet='0'}"
                BackgroundColor="Transparent"
                DotPlacement="None"
                EnableLooping="False"
                EnableSwiping="True"
                HorizontalOptions="CenterAndExpand"
                ItemTemplate="{StaticResource ItemTemplate}"
                ItemsSource="{Binding Boardings}"
                NavigationDirection="Horizontal"
                SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                VerticalOptions="CenterAndExpand"
                WidthRequest="{core:OnPlatformOrientationDoubleValue Default=400,
                                                                     AndroidPhonePortrait=400,
                                                                     AndroidPhoneLandscape=200,
                                                                     iOSPhonePortrait=400,
                                                                     iOSPhoneLandscape=200}">
                <rotator:SfRotator.Behaviors>
                    <behavior:SfRotatorBehavior />
                </rotator:SfRotator.Behaviors>
                
            </rotator:SfRotator>

            <!--  Next button  -->
            <buttons:SfButton
               Margin="0,0,0,35"
                Grid.Row="3"
                Command="{Binding NextCommand}"
                
                Style="{StaticResource SfButtonStyleForOnbBoarding}"
                Text="{Binding NextButtonText}" />
        </Grid>
    </ContentPage.Content>
</ContentPage>
