<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:listView="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:border="clr-namespace:Syncfusion.XForms.Border;assembly=Syncfusion.Core.XForms"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="EGAZT.Views.NewDesign.ZakatDeregistration.ZakatRegistrationDetailsListPageView"
             xmlns:ResourceManager="clr-namespace:EGAZT" NavigationPage.HasNavigationBar="False"
             x:Name="ZakatReg"
             BackgroundImageSource="partials_background">
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\..\SyncFusionEnabledViews\CommonStyle\StylesNewUI.xaml" />
                <ResourceDictionary Source="..\..\SyncFusionEnabledViews\CommonStyle\Styles.xaml" />
                <ResourceDictionary>
                    <Style x:Key="ArrowImageForArabicStyle" TargetType="Image">
                        <Setter Property="Rotation" Value="180" />
                    </Style>
                    <Style x:Key="ArrowImageForEnglishStyle" TargetType="Image">
                        <Setter Property="Rotation" Value="0" />
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>          
            </ResourceDictionary>
    </ContentPage.Resources>
        <ContentPage.ControlTemplate>
        <ControlTemplate>
            <Grid>

                <StackLayout>
                    <Grid Padding="15,5,15,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20*"/>
                            <ColumnDefinition Width="60*"/>
                            <ColumnDefinition Width="20*"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="1" Grid.Row="0"   Text="{x:Static ResourceManager:AppResources.TinDeregistrationRegistrationDetails}" Style="{StaticResource SectionTitleWhiteBold}" HorizontalOptions="Center"/>

                        <Image Grid.Row="0" Grid.Column="0" IsVisible="{Binding IsBackStepButtonVisible}" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand"
                               Source="arrowLeftwhite" x:Name="Image_BacKIcon"  Style="{DynamicResource BackButtonArrow}" >
                            <Image.GestureRecognizers>
                                <TapGestureRecognizer Command="{TemplateBinding BindingContext.GoBackBtnTapped}"/>
                            </Image.GestureRecognizers>
                        </Image>
                    </Grid>
                    <yummy:PancakeView  VerticalOptions="FillAndExpand" CornerRadius="14,14,0,0" Padding="0">
                        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"  Margin="0" 
                  Padding="0,0,0,0" BackgroundColor="White" x:Name="VATRMainGrid">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="100*" />
                            </Grid.RowDefinitions>
                            <ContentPresenter VerticalOptions="FillAndExpand"
                        HorizontalOptions="FillAndExpand"
                                              Padding="0,0,0,20"
                        Grid.Column="0"
                        Grid.Row="0" BackgroundColor="White"/>
                        </Grid>
                    </yummy:PancakeView>
                </StackLayout>

            </Grid>
        </ControlTemplate>
    </ContentPage.ControlTemplate>

    <ContentPage.Content>
         <listView:SfListView x:Name="outletDecisionOptionsListView" ListViewCachingStrategy="RecycleTemplate" HorizontalOptions="Center" IsScrollBarVisible="False" IsScrollingEnabled="True"  VerticalOptions="FillAndExpand" Grid.Row="2" Grid.Column="0" AutoFitMode="Height"  Grid.ColumnSpan="2" ItemSpacing="5"
                                      ItemsSource="{Binding ZakatRegListData}" SelectedItem="{Binding SelectedOutletOption, Mode=TwoWay}"  SelectionMode="Single"
                                      SelectionBackgroundColor="Transparent" SelectionChanged="registrationDetailsListView_SelectionChanged">
                        <listView:SfListView.ItemTemplate>
                            <DataTemplate>
					             <border:SfBorder   Margin="16,10,16,0"  Style="{StaticResource GreyBorderFrameWithShadow}">
                                    <Grid Padding="10">
                                        <Grid.RowDefinitions >
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                         <Image Margin="10,0,10,0" Grid.Row="0" Grid.Column="0" Source="{Binding ZDImageSource}" WidthRequest="20" HeightRequest="20"
                                        HorizontalOptions="Start" VerticalOptions="Center">
                                        </Image>
                                        <StackLayout Grid.Row="0" Grid.Column="1" HorizontalOptions="Start" VerticalOptions="Center">
                                            <Label Text="{Binding ZDTitle}" Margin="5,-4,5,0" Style="{StaticResource ValueLabelBlack}" HorizontalOptions="Start" FontFamily="{StaticResource SSTArBold}" VerticalOptions="Center" />
                                        </StackLayout>
                                        <Image Grid.Row="0" Grid.Column="2" Source="{Binding ArrowImageSource}"  Margin="5,0,5,0" HorizontalOptions="Start" VerticalOptions="Center">

                                             <!--<Image.Triggers>
                                                 <DataTrigger TargetType="Image" Binding="{Binding IsArabic}" Value="false">
                                                    <Setter Property="Rotation" Value="180" />
                                                </DataTrigger>
                                                 <DataTrigger TargetType="Image" Binding="{Binding IsArabic}" Value="true">
                                                    <Setter Property="Rotation" Value="0" />
                                                </DataTrigger>
                                            </Image.Triggers>-->
                                        </Image>
                                    </Grid>
                                </border:SfBorder>
			                </DataTemplate>
                        </listView:SfListView.ItemTemplate>
           </listView:SfListView>
    </ContentPage.Content>
</ContentPage>
